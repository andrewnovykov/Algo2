function compressB(boxes, boxes2) {
    boxes.forEach(function (boxes) {
        console.log(boxes);
    }) //O(n)

    boxes2.forEach(function (boxes) {
        console.log(boxes);
    }) //O(n)
}

// O(n + n)